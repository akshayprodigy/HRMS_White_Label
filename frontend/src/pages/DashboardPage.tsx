import { Typography } from '@mui/material';

export function DashboardPage() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Dashboard
      </Typography>
      <Typography color="text.secondary">Placeholder page.</Typography>
    </div>
  );
}
